# CommandeIci recovery baseline

Captured: 2026-07-19, Europe/Paris
Environment: isolated macOS worktree
Commit: `38d7f48`

## Repository state

- `origin/main` was fetched before the worktree was created.
- The authoritative application code has not changed since commit `f6005d9` on 2026-04-19; later commits only update prospecting logs.
- The original checkout contains unrelated user-owned prospecting files. They are not present in this worktree.

## Reproducibility

`npm ci` initially failed in the restricted network environment with repeated npm registry `ENOTFOUND` errors and npm 11's `Exit handler never called` wrapper error. The same lockfile installation succeeded with approved network access:

- 597 packages installed;
- 598 packages audited;
- exit code 0.

## Automated checks before changes

| Check | Result | Baseline finding |
|---|---:|---|
| `npm test` | PASS | one example test only; no product behavior covered |
| `npm run lint` | FAIL | 298 findings: 250 errors and 48 warnings |
| `npx tsc -p tsconfig.app.json --noEmit` | FAIL | 145 TypeScript errors |
| `npm run build` | PASS with warnings | Vite does not typecheck; multiple chunks exceed 500 kB and browser data is stale |
| locale JSON parse | PASS | all 14 files are valid JSON |
| locale key parity | FAIL | French has 1,387 keys; every other locale has 1,383 |
| `npm audit` | FAIL | 24 vulnerable packages: 1 low, 9 moderate, 12 high, 2 critical |

The two critical audit paths are direct dependencies `jspdf` and `vitest`. High-risk paths include direct dependencies `react-router-dom` and `vite` plus transitive packages.

## Supabase source-of-truth check

The application configuration references the historical project `rbqgsxhkccbhqdmdtxwr`. A read-only remote type generation request returned `Resource has been removed`. A read-only project inventory confirmed that no CommandeIci project is present among the accessible active projects.

Consequences:

- the live backend cannot currently satisfy application requests;
- remote schema, data, functions, secrets, auth settings, storage, cron, and webhook state cannot be treated as recoverable from the service;
- the versioned migrations and function source are now the only available backend reconstruction source in this workspace;
- a new or restored Supabase project is required before live delivery;
- no production data restoration can be claimed without an independent backup not yet identified.

## Configuration and security observations

- `.env` is tracked in Git and has been present since early repository history. It contains only public frontend Supabase configuration keys by name, but the practice must be reviewed and documented.
- Historical project documentation contains credential material and stale Shopify instructions. Secrets must not be copied into new documentation or commits.
- The public marketing site states that customer payments are processed by Stripe. The application order path only records a `payment_method`; current Stripe functions cover the restaurant subscription, not customer order settlement.
- The scheduled prospecting workflow remains versioned but no bulk send is authorized during recovery.

## Baseline verdict

The project builds but is not releasable from this state. The green Vite build is insufficient evidence because typecheck, lint, dependency security, localization parity, behavior coverage, and the backend source of truth are all red or absent.
